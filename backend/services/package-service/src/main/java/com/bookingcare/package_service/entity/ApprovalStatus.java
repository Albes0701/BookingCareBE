package com.bookingcare.package_service.entity;

/**
 * Approval status values mapped from the {@code approval_status} enum type in the database.
 */
public enum ApprovalStatus {
    DRAFT,
    PENDING,
    APPROVED,
    REJECTED
}
